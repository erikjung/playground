/**
 * All that happens in this file is observations and UI reactions to the global state
 */
define([
  'core/state',
  'control/status'
],
function (state) {
  var status = $('.js-status').status()
})